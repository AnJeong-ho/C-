1.算法的基本成分：基本的操作及控制结构
其中：基本的操作有:(1)表达式以及给变量赋值(2)读(输入)(3)写(输出)
基本的控制结构有：(1)顺序控制结构(2)分支控制结构(4)循环控制结构(4)函数调用(5)函数返回
2.一个有效的算法应具有以下特点：
（1）有穷性：在有限步内终止，且在合理运算时间范围内
（2）确定性：每一个步骤都应该是确定的，而不是模棱两可的，如对于分母应该考虑等于0的情况，对于根式应该考虑小于0的情况
（3）有效性：如x->x,a/0显然无效，不应该出现
（4）0个或多个输入
（5）一个或多个输出
3.程序设计语言：
（1）机器语言，由0和1组成，计算机可以直接识别
（2）汇编语言：是符号化的机器语言，要经过汇编程序翻译为机器语言计算机才能识别。与机器语言是一一对应的。与硬件密切相关，不便于移植。
（3）高级语言：如C，C++，C#，Java，Python等等，经编译器将整个程序翻译为机器语言，再执行(C,C++)。或经解释器取出单条指令翻译为机器代码后执行(Python),一条语句可能被翻译为多条机器指令
4.程序：
（1）一个C语言程序由一个或多个编译单元组成，每个编译单元是一个源文件
（2）一个编译单元由多个顶层声明组成，每个顶层声明可以是声明或者函数定义
（3）声明包括类型定义（class，struct等等)，变量声明，宏等等
（4）必须有且仅有一个主函数，C99规定返回值必须为整型
5.程序执行步骤
（1）编写：得到.c文件
（2）编译：源代码->目标代码，子过程有预处理，编译(成为汇编语言),汇编(成为机器语言)。
（3）链接：将各个目标文件即.obj文件以及需要的库函数链接到一起，成为可执行.exe文件
（4）执行：计算机识别机器代码进行完成任务
