1.每个字符有两种形态：
（1）可视形态：显示在屏幕上可以看见的
（2）存储形态：二进制代码
2.标识符：
（1）不能与关键字重名
（2）只能由数字字母(分大小写)和下划线构成，不能有空格字符
（3）只能以字母或下划线开头
3.间隔符：
用以区分不同标识符，如空格，行结束符等等
多个连用相当于一个，在任何词法单位内部不允许出现间隔符，如317不能写成3 17，>=不能写为> =
4.注释：
//开始至本行结束，为行注释
/*这之间均为注释，称为段落注释*/
5.关于浮点数的误差：
（1）3.7实则为3.69999...，故*10取整会发生较大误差
（2）两个几乎相等的值相减，可能引起较大误差，故判断相等，用fabs(a-b)<=1e-6判断
（3）除数的绝对值较小的话，除数可能会溢出
（4）积累误差：n个1/n相加，结果未必为1
6.十六进制开头用0x表示，八进制开头用0表示
7.两整型相除，商自动取整，分子*1.0变为浮点型即可避免
8.字符转义符：'\52'是一个转义字符，代表ASCII值八进制为52的字符，即'*','\x52'为十六进制
9.宏定义是原封不动地替换，注意不要有多余字符，如分号
10.关于隐式类型转换：
long double        ← 最高
    ↑
double（3.14缺省类型为double而不是float）
    ↑
float
    ↑
unsigned long long
    ↑
long long
    ↑
unsigned long
    ↑
long
    ↑
unsigned int       ← unsigned char、unsigned short 提升到这里
    ↑
int                ← char、short 提升到这里（最低）
11.语句分为简单语句和结构语句
（1）前者有空语句表达式语句return语句break语句continue语句goto语句
（2）后者有复合语句，分支语句(if,switch),重复性语句（do,for,while）
12.%md,占用m格右对齐，%-md，是左对齐，%0md,前面自动补0

