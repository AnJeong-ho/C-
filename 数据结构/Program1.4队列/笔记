1.front指向队首元素，rear指向队尾元素的下一个位置
当且仅当front==rear，队空。
入队：A[rear++]=K,K元素入队，后进的是队尾，出队的是队首
出队：return(front++);不用实际删除，front右移即可，return语句同pop返回T类型类似
假溢出：rear指向maxsize位置，但由于front右移出队了一些元素，实际并未满
解决方法：循环队列
2.循环队列：
出队：不是简单的front+=1;而是又整除了总长m，使front总落在0到m-1以内：
int K=A[front];
front=(front+1)%m;
return K;
入队：A[rear]=K;rear=(rear+1)%m;//由于rear指向的不是队列元素，相当于光标，故直接填充新的元素即可
问题：对空和队满都是front==rear，易混淆
方案一：引入变量count记录队列元素个数，初始化时多了count=0;入队出队要对元素+-1,队满的判定为count==m;
方案二：队尾最后一个位置永远空着,即rear所指一定为空，空间换时间
队满的判定：(front+1)%m==front;
数量判定：(rear-front+m)%m,因为可能rear在左，front在右，那就差个m了
3.队列和栈的区别就是栈的出口即为入口，而队列是先进先出
4.//深度优先搜索：一条路走到黑，用回溯法实现
   //广度优先搜索：地毯式搜索，路径长度递增，直到终点，常用于寻找最短路径
5.队列的应用：缓冲
