
<<1.对于链表的操作有：
    创建（头插法），查找（按值查找，按序查找，找前驱），插入，删除
————————具体代码见："D:\Progarmming\DataStructures\Project1.1_链表"
<<2.对比顺序存储和链式存储
1）空间上：顺序存储的空间是固定的，很多空间是闲置的，而链式存储的空间是动态申请的，但是指针会产生额外的开销。
2）时间上：若基于下标查找，顺序表是O（1），而链表是O（n），即从头到尾依次查找；若插入或删除，则反之，链表效率更高。
<<3.需要特别注意边界
1）链表是否为空，if(p==NULL)return;若更严谨，判别head非空，若head为哨位结点则无需判别
2）链表是否只包含一个结点（判断回文或者重排时，单结点是特殊情况）
3）当p为头结点或者尾结点时怎么处理
<<4.链表的双指针技巧——————————具体代码见：
"D:\Progarmming\DataStructures\Project1.2_链表进阶"
1）FindMiddle:创建两个步频二倍关系的指针，使快指针从头到尾移动可找到中点
2）FindLastKth:两结点相距为K，前结点由head出发，当后结点终止于NULL时，前结点即为所求
3）Reverse:用到p及其后继结点q，不断是q更新为head,循环移动p直到p->next==NULL即可
4）Cross:要找到交点，找到更长的链表，先移动一段距离使两链表对齐，while(p1!=p2)
5）detectCycle:创建两个步频二倍关系的指针,若有环，必相遇，否则快指针终止于双指针归并，注意系数抵消和哨兵结点NULL
6）detectCycleEntrance:步频相同指针分别由head和相遇点出发，根据数学关系，必然在入口处相遇
7）isPalindrome:后半段切开后翻转，判断两链表是否完全一样
8）Reorder List: 快慢指针找到链表中间结点，将后半部分链表反转，将前半段和反转后的后半段交替连接
9）Polynomial Addition(多项式相加):双指针归并，注意系数抵消和哨兵结点
