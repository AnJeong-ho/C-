1.指针top初始是-1，数据从0开始依次填入，top指向最顶层最新的数据，对应数组角标
2.栈的清除是逻辑清除而不是物理清除，数组里的旧数据依然存在，只是top指针变小了，旧数据实际上只能被覆盖而不是被清除
3.为什么清除函数pop()返回类型是T而不是void：
————因为调用者通常需要知道被移除的元素是什么。
————如果返回 void，你弹出元素后就没法知道它是什么了——这个设计会非常不方便。
4.防御式编程——assert()断言：
————配合bool型的判空函数empty()和判满函数full()使用，若参数表达式是假，则调用abort()函数终止程序运行
————用于检查执行前后必须具备的状态
5.补：1）string类型支持+和+=运算，不支持-运算
注意：string s2 = "Hello" + s; // ✅ const char* + string
              string s3 = "Hello" + " World" + s; // ❌ 编译错误！// 原因："Hello" + " World" 是两个 const char* 相加，不支持
              string s4 = string("Hello") + " World" + s; // ✅ 显式转换
              2)如何实现数字向对应的字符转换：+‘0’即可，因为0的ASCII码为48，加上了ASCII正好一一对应。
6.进制转换，代码见源文件。
1）注意先判断n==0
2）根据正负型初始化string类型数据
3）取n的绝对值
4）用while循环压栈，前提设为n>0,
5）用while循环清栈，不断更新string数据，注意+'0'，前提用!empty()
6）若进制大于10，则更新时候，用字符数组char digite[]更新，字符数组中是K进制数字
7.括号匹配
// 若只有一种括号，遇到左括号+1，右括号-1，结果==0即可
//用字符数组pair完成括号匹配，如下
//pair['(']=')';pair['[']=']';pair['{']='}';
//遇到左括弧，压栈，遇到右括弧，若匹配失败，返回假
//拓展：加上嵌套顺序为{ → [ → ( → <}。压栈时，必须保证左侧没有低级括号或者括号已经被抵消，这是拓展与基础的不同之处，关键在于sign[]的运用。
8.中缀表达式转后缀表达式：
    if遇到操作数，直接送进队列
    if遇到左括弧，入栈
    if遇到右括弧，弹出直到弹出左括弧为止
    if遇到运算符，把运算级相等以及更高的弹出至队列直到栈空或者栈底为左括弧，再入栈//每个元素最多入栈一次，出栈一次，入队一次，时间复杂度O(n)
    if遇到空，弹栈直到栈空
9.中缀表达式求值
1）转为后缀表达式再求值
2）将两过程合并，当运算符要进入队列时，立刻弹出两个操作数进行运算
此时的队列叫作操作数栈，只存操作数，运算符进不来

